import React from 'react';
import ReactDOM from 'react-dom/client';
import NewApp from './NewApp';  // Should point to NewApp

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<NewApp />);
